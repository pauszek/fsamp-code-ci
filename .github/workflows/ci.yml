name: CI
on:
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened, ready_for_review ]
  push:
    branches: [ main ]

jobs:
  build-lite:
    uses: ./.github/workflows/build-lite.yml@main
    secrets: inherit
