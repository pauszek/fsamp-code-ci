name: CI
on:
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened, ready_for_review ]
  push:
    branches: [ main ]

jobs:
  call-java:
    uses: ./.github/workflows/build-lite.yml@main
    secrets: inherit
