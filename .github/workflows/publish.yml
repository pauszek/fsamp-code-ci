name: Publish Shared Library on Release

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven
          server-id: github
          settings-path: ${{ github.workspace }}

      - name: Read version from release.version
        id: ver
        shell: bash
        run: |
          set -euo pipefail
          [[ -f release.version ]] || { echo "release.version not found"; exit 1; }
          echo "value=$(tr -d ' \n' < release.version)" >> "$GITHUB_OUTPUT"

      - name: Build and publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REVISION: ${{ steps.ver.outputs.value }}
        run: mvn -B clean deploy -DskipTests -Drevision="$REVISION" -s "$GITHUB_WORKSPACE/settings.xml"